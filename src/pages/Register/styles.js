import styled, { css } from 'styled-components';

export const Container = styled.section`
${({ theme }) => css`
  .accountCreated {
    button {
      margin-top: 100%;
    }

    a {
      margin-top: 100%;
    }
  }
`}
`;
